{
    "name": "chrisesser/keel",
    "description": "A PHP 8.4 application baseline with users, organizations, auth and billing already built. Clone it and build on top.",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Chris Esser",
            "email": "esser32@gmail.com"
        }
    ],
    "require": {
        "php": ">=8.4",
        "ext-json": "*",
        "ext-mbstring": "*",
        "ext-openssl": "*",
        "ext-pdo": "*",
        "stripe/stripe-php": "^20.3"
    },
    "suggest": {
        "ext-sodium": "Required only for Framework\\Security\\Crypto — encrypting secrets at rest, e.g. TOTP seeds",
        "ext-curl": "Required only for the Mailgun and Twilio providers"
    },
    "autoload": {
        "psr-4": {
            "Framework\\": "src/",
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "minimum-stability": "stable"
}
